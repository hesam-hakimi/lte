Interactive sudo salt-call pillar.get secrets:tungsten_primarykey succeeds and returns a non-empty value, but the unattended wrapper fails with exit code 66 because sudo -n is denied for the runtime account. Please validate the AutoSys service account and provide narrowly scoped NOPASSWD authorization for retrieval of the four approved Salt pillar secret names. No broad salt-call or shell sudo access is requested.
