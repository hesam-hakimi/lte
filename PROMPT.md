# Prepare the retained 0.3.160 build for internal team testing

TASK_ID: ETL-0915-TEAM-INTERNAL-TEST-KIT-PREP01
STATUS: PREPARED_FOR_OWNER_SUBMISSION; not executed by ChatGPT.
AUDIENCE: Hesam's direct technical teammates doing experimental development.
SCOPE: Prepare a local, reviewable internal test kit using the unchanged qualified VSIX, a minimal synthetic example, English instructions and an accurate limitations record.

On owner submission, perform this preparation only. Use a new task-owned staging/evidence directory. Keep all communication and artifacts English. Use text, DOM/accessibility and filesystem evidence only; no screenshots, video, OCR or vision. This task does not send or publish files, install a candidate, execute product tests, launch hosts, invoke runtime models, perform product writes or run Databricks/DBFS operations.

## 1. Intake and current state

Read this complete brief and the supplied references/09_AGILE_REPAIR_AND_VERIFICATION_CONTRACT.md v1.2. Authenticate its 23,684 bytes and full SHA-256 against HASHES.json, then record its real staged Windows path. Preserve the source and closed predecessor reports. This closes a prospective reference-delivery gap; it does not amend historical compliance or reopen the completed header repair.

Use existing published task metadata to locate the actual checkout and candidate evidence. The known checkout locator is C:\repos\etl-extension\etl_fw2\recovery-extension-product-0.3.147. Read applicable local instructions. For architecture/safety details, reuse authentic Windows-staged 02_PROJECT_AND_ARCHITECTURE.md and 05_SAFETY_AND_WORKING_MODEL.md; if a required detail is unavailable, identify that exact input rather than inventing it or searching whole drives/history.

Check whether this exact preparation task already exists. Resume its own unfinished work or report its completed kit; do not dispatch duplicate preparation. Claim only this task's outputs and necessary exclusive operations. Product source, installed profiles and closed evidence are read-only inputs.

The following later owner reports supersede older running/STAGED wording. Authenticate only the local records needed for this kit:

- ETL-0914-VALIDATION-CODE-DELIVERY-QUALIFY01 delivered and independently qualified installed 0.3.160 with recorded R1/R2 inputs. R1 reached a genuine final Cancel; R2 was blocked, with the intended path-in-sql diagnostic and a separate sourced-view error. Rejection was observed, but the SQL diagnostic was not demonstrated to be its sole cause. Public blocked output remains message-only; internal code preservation has separate reviewed composed-test and source/package evidence.
- ETL-0915-JSON-PAIR-WRITE-AND-REUSE01 performed one authorized CREATE batch. The .json HOCON job and .json keyed-HOCON SQL include were created and read back byte-exactly; no other consumer files changed. The single write allowance is SPENT. Installed read-only tools were shown to read the job; their reading of included SQL was not demonstrated. Write-path include resolution is separate evidence. This was recorded-input replay, not fresh natural-language generation on .160 or Databricks execution.
- Coverage registration, real-validator replay and timing work are complete. The writeFlow filesystem repair is APPLIED; its matching isolated matrix was 17 passing / 9 failing before, a behavioral red of 17 / 9, and 31 / 0 after. All 26 original cases remain. GUI execution was unperformed and normal GUI/integration registration was preserved. This test-only repair does not by itself require a new VSIX.
- Overnight closeout is VERIFIED and closed. Its corrected lane helper is a task-owned copy-forward artifact, not a shared repository helper. Do not adopt it or repeat the audit here.
- ETL-0915-MAINTAINER-DEVELOPER-FRONTMATTER-REPAIR01 is APPLIED with final VERIFIED_WITH_LIMITATIONS and ownership released. It removed exactly ASCII '78' (bytes 37 38) from the beginning of developer.agent.md: 4952 -> 4950 bytes, with the remaining bytes preserved. The reported actual frontmatter assertion changed from no match to match. validate-workflow.mjs never reads this target, so its unchanged exit 0 is not evidence for that file. The expanded frontmatter check reported 12 failing -> 11 failing; the consumer suite was not rerun and still has a known subsequent failure. These are not new full-suite results or counts to merge with other populations. Host loading/discovery/routing remain unqualified.
- The header review used a disclosed generic independent reviewer because the named Verifier was unavailable. Its three returns were transcriptions with artifactAuthoredByReviewer: false and byteExactToolCapture: false. Preserve the final limitations, including retained R-2, as defined by the actual review; do not upgrade the verdict. The absence of the original contract on Windows was reported as TOOLING_GAP.
- Latest reported preservation state: worktree version 0.3.147, dirty count 78 after the repaired header matched HEAD, shared out 2041 files, .tsbuildinfo.test 158611 bytes, installed 0.3.160 unchanged. The header task compared 23 .github files to an independent reference copy: 22 unchanged, one intentional change. This new observation does not strengthen the older overnight 5/23 historical byte-proof limitation.

## 2. Authenticate the exact distributable

Resolve the retained .160 VSIX and its accepted full digest, source/package records and installed qualification from the named delivery/write tasks. Verify the archive against those records. A version string, current dirty worktree or an installed directory alone is insufficient provenance.

Copy that unchanged VSIX into the new kit. Do not rebuild from the current recovery worktree, recreate a VSIX from installed files, edit package metadata, bump the version or silently substitute a candidate. If the accepted archive cannot be authenticated, complete the useful documentation as DRAFT and identify the exact artifact gap.

Inspect the actual archive layout and existing manifests to identify packaged consumer assets, dependencies and supported prerequisites. Preserve the reconciled counting scopes; differing archive/installed/resource totals are not automatically missing files. Confirm whether the later test and maintainer edits are outside this retained distributable before describing their effect. Keep the maintainer Orchestrator/Developer instructions distinct from the consumer ETL assets.

Use only the VSIX and explicitly selected synthetic fixture/documentation inputs. Do not copy whole worktrees, task evidence roots, chat logs, profiles, credentials, token stores, .env files, caches or real business data into the team kit. Derive setup instructions from actual package declarations and retained successful installation records; do not invent minimum versions, commands, agent IDs or a PAT-based login workaround. Each tester uses their own supported account authentication.

## 3. Build the small reviewable kit

Include the verified VSIX, a machine-readable digest/file manifest, one README, a minimal sample workspace with explicit setup instructions, a concise TEST_CASES.md, KNOWN_LIMITATIONS.md and a bug-report template. Keep paths relative and short. The template should capture candidate digest/version, environment, test case, expected/actual outcome and relevant text logs with secrets removed. Do not ship the maintainer reference package as an automatic consumer instruction set.

Reuse the qualified synthetic R1 dependency context where it is suitable for teammates. Inspect and enumerate the necessary non-secret inputs instead of copying a historical consumer wholesale. Record any portable path/environment adaptation; changed fixtures are not byte-identical to historical inputs. For a CREATE scenario, both intended output destinations must initially be absent. If including the already-written pair as a read-only example, keep it separate from that fresh workspace and label its role.

Preserve the representation contract: new job names use .json with HOCON content; .conf is legacy-compatible; supported include suffixes are not categorically banned; the SQL include is a keyed document assigning SQL text, not an artifact path in sql. Framework source-view identity is table.name when present, otherwise the sourceList key; alias is a column rename.

Define initial team scenarios through actual supported product entrypoints:

1. Install the identified VSIX on a teammate's machine, verify the active version and observe normal consumer-asset discovery. Mark this as a future team observation, not a result already established on that machine.
2. Read the supplied HOCON-in-.json example and record the actual module/discovery output. Do not promise successful include-read observability that the installed evidence did not establish.
3. Inspect a supported R1-style proposal through fresh preview, bound continuation and genuine final Cancel. Expected: intended CREATE destinations, preserved SQL/filter/mappings, and no write. Do not ship an unattended approval driver or reuse a historical preview.
4. Inspect the invalid path-in-sql R2 case with its authentic context. Expected: rejection before preview/approval, while retaining all returned diagnostics. Do not label an unrelated setup failure a passing validation test.
5. Offer one clearly labeled exploratory natural-language scenario using synthetic business facts. Capture actual generation and preview behavior and end at Cancel. Do not present the recorded .160 replay or .159's ALIAS_PRESENT-only live result as proof this fresh request will pass.

Actual CREATE/overwrite/delete testing and Databricks execution are outside this initial preparation. Describe a later optional CREATE test only as requiring its own fresh bounded authorization, fresh preview and isolated destinations. All earlier .154/.155/.160 allowances remain SPENT; no permission transfers to a teammate or automation from those records.

## 4. Validate composition and expose relevant limits

Perform only local static preparation checks: copied VSIX digest correspondence, manifest completeness, archive readability, sample paths/dependency consistency and guide accuracy against the real interfaces. Reuse valid candidate qualification and accepted fixture results where their inputs match. Mark adapted examples and teammate-machine behavior as not executed. Do not run a compile, test-suite campaign, new installation, host/model flow or product write just to prepare the kit.

Read the actual retained definitions of item E, F-ROOT-1, ledger identity limits, the check-to-write race and relevant unexercised branches before deciding the supported test scope. Expose any concrete limitation affecting the proposed cases and constrain or defer that case accordingly. Do not waive an unresolved applicable gate, renew the expired quarantine, demand every unrelated backlog item be fixed or turn missing evidence into a PASS. Keep the other seven historical failures, overnight failures and the header's 11 remaining checks separately attributed.

Keep natural-language generation, read-only include observability, GUI/provider equivalence and Databricks/runtime/DBFS qualification limits explicit. Phase H and remaining .github/frontmatter findings are not repair scope here. Determine their actual relationship to this unchanged VSIX before making a blocking or non-blocking claim.

Use one focused independent read-only review of the kit's artifact identity, composition, setup/test instructions and evidence claims. Reuse existing product reviews; review only new packaging/documentation claims and applicable limits. Correct in-scope preparation mistakes and re-review their delta. Disclose the actual reviewer and persistence method rather than claiming an unavailable named Verifier or byte-exact capture.

## 5. Return the kit for owner inspection

Create the final outer ZIP in task-owned staging; keep the enclosed VSIX unchanged. Record the outer ZIP digest and a complete content manifest, with clear self-exclusion for any manifest hash. Store internal source/evidence provenance separately from the teammate-facing kit where it contains machine-specific paths or logs.

Return the exact Windows kit location, digest, copied candidate identity, included setup/sample files, reviewed scenario scope, reused versus new checks, actual reviewer disposition and any concrete remaining blocker. Use PREPARED_FOR_OWNER_HANDOFF only if the kit and its claims support that status; otherwise return the accurate DRAFT/BLOCKED result and useful completed artifacts.

Do not email, message, upload, publish, install or start team testing from this task. Do not edit the active worktree, its .github files, the applied writeFlow repair, shared out/build-info, installed .160, profiles, consumers or closed reports. Preserve original attempts and release only this task's ownership. No production release, deployment, all-green or full-readiness claim follows from this internal-test preparation.
