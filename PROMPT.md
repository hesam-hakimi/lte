Before any further connection, compare the exact endpoint used by the previously successful DEV Linux run with the current target:

https://penhubpat.td.com/aws/services/AwsService

Use the successful run’s configuration or execution evidence. Report both exact URLs and their environment labels. If the earlier endpoint is unavailable, state that it remains unknown.

Do not assume DEV and PAT are equivalent, change endpoints, or make another network call. Keep credentials and configuration secrets out of the report.
