GitHub CLI authentication is now complete.

Verify authentication and the remote feature-branch HEAD. Check whether a PR already exists for feature/clue-env-lifecycle-20260924 against main.

Create one draft PR if none exists; otherwise update the matching existing PR using the prepared body. Clearly distinguish the three verified implementation areas from the unresolved business policy for profile compatibility. Include the same-host limitation and reported test results.

Do not merge, force-push, change the base, rerun completed tests or modify application code for this PR-only step.

Return the PR URL, draft status, head/base and any required review checks.
